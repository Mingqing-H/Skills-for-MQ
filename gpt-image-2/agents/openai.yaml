interface:
  display_name: "GPT Image 2"
  short_description: "Generate or edit images through local Codex CLI."
  default_prompt: "Use $gpt-image-2 to generate an image from my prompt."
policy:
  allow_implicit_invocation: true
